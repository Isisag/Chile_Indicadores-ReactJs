import React from 'react';
import {Card} from "@material-ui/core"


const Indicator = ({bitcoin, uf}) => {

  return(
    <Card>
      <p> im a single indicator </p>
      {/* <p> {uf} </p> */}
    </Card>
  )
};

export default Indicator;
